// Notes =======================================================================
// Print specific styles (based on HTML5-Boilerplate)
// - Backgrounds should be specified within @media screen
// - NO blanket overrides in `@media print`, just take care:
//   - For example, specify backgrounds in @media screen only
//   - The only downside is "print to pdf files won't be as pretty :(
//
// #1: Don’t show links for images, or javascript/internal links
// #2: Tables - http://css-discuss.incutio.com/wiki/Printing_Tables

@media print
  a,
  a:visited
    color: #000
    text-decoration: underline

  a[href]:after
    content: " (" attr(href) ")"

  abbr[title]:after
    content: " (" attr(title) ")"

  // #1
  a[href^="#"]:after,
  a[href^="javascript:"]:after
    content: ""

  pre,
  blockquote
    page-break-inside: avoid

  // #2
  thead
    display: table-header-group

  tr,
  img
    page-break-inside: avoid

  p,
  h2,
  h3
    orphans: 3
    widows: 3

  h2,
  h3
    page-break-after: avoid
