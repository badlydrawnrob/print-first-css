// Baseline grid ===============================================================
// Material design `4sp/px` baseline, with our `16sp/px` default line-height
// - Link: http://bit.ly/2lYjdkt
// - Typographic rhythm: http://bit.ly/2mnBSri
// - Background gradient: http://bit.ly/2lE4pMV
// - #1: Use `$align` to nudge the grid to wherever you need it.
//   - We're using `rem`, so it isn't pixel perfect. Properly `$align`ed,
//     type lies over baseline.

baseline-grid($depth = -1, $align = 0)
  position: relative

  &::before
    content: ''
    position: absolute
    z-index: $depth

    // #1
    if $align
      top: $align

    display: block
    width: 100%
    height: 100%
    background: linear-gradient(0deg, alpha(#000, 30%) 1px, transparent 1px, transparent),
                linear-gradient(0deg, alpha(#000, 20%) 1px, transparent 1px, transparent)
    background-size: auto $spacing.base, auto $spacing.quarter
